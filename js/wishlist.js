 // Function to retrieve card from cookie and display it
 /*function displayCardFromCookie() {
    var cookieData = getCookie("wishlistCard");
    console.log("Cookie data:", cookieData);
    if (cookieData) {
        var cardContainer = document.getElementById('wishlist-grid');
        cardContainer.innerHTML = decodeURIComponent(cookieData);
    }
}

// Function to get cookie value by name
function getCookie(name) {
    var cookieValue = "";
    var cookies = document.cookie.split(';');
    cookies.forEach(function(cookie) {
        var cookiePair = cookie.trim().split('=');
        if (cookiePair[0] === name) {
            cookieValue = cookiePair[1];
        }
    });
    return cookieValue;
}

// Display the card from cookie when the page loads
window.addEventListener('load', function() {
    displayCardFromCookie();
});*/